// given base and height calculates area of a triangle
// things to add would be to make sure these are numbers, and if its a valid triangle 
module.exports = function (base,height){
    return ((base *height)/2);
};

// test code manually
//console.log(triangleArea(3,4));